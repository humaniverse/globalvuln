% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collate-indices.R
\name{collate_indices}
\alias{collate_indices}
\title{Collate selected humanitarian vulnerability indices}
\usage{
collate_indices(indices, format = c("wide", "long"))
}
\arguments{
\item{indices}{A non-empty character vector of index identifiers. Available
identifiers are \code{inform_risk}, \code{inform_severity}, \code{underfunded_crisis},
\code{oecd_fragility}, \code{worldrisk}, \code{nd_gain}, \code{hdi}, \code{mpi}, \code{ghi}, \code{ghs},
\code{wps}, \code{un_mvi}, \code{debt_distress}, \code{searo}, \code{disaster_displacement}, and
\code{internal_displacement}. Identifiers must be unique and their supplied
order is preserved.}

\item{format}{Output layout: \code{"wide"} (the default) or \code{"long"}.}
}
\value{
A data frame. Wide output has one row per country, four geography
columns, \verb{*_score}, \verb{*_rank}, and \verb{*_decile} columns for each selected
index, applicable index-specific fields, and five country summary columns.
Long output has one row per country-index combination, the 19 fields
documented in \link{individual_indices}, and the same five summary columns.
}
\description{
Combine any selection of the package's individual index datasets in either
wide or long form. All 195 United Nations member and observer states are
retained, including countries outside an index's published coverage.
}
\details{
\code{top_10} and \code{top_20} identify ranks less than or equal to 10 and 20,
respectively; they do not refer to deciles. Because ties receive the minimum
rank, more than 10 or 20 countries can meet a threshold for an index.

The summary fields are:
\itemize{
\item \code{indices_ranked_count}: selected eligible indices for which the country
has a non-missing rank.
\item \code{top_10_count} and \code{top_20_count}: the number of those ranks meeting each
threshold.
\item \code{top_10_proportion} and \code{top_20_proportion}: each count divided by
\code{indices_ranked_count}.
}

A country with no available eligible rank has zero counts and missing
proportions. Missing scores are not treated as ranks outside the thresholds.
In long output, country summaries are repeated on every selected index row.

Wide output includes \code{ghi_score_label}, \code{mpi_reference_year},
\code{debt_distress_class}, and \code{debt_distress_ordinal} when their corresponding
indices are selected.
}
\examples{
selected_wide <- collate_indices(
  c("inform_risk", "hdi", "mpi")
)
selected_wide[
  selected_wide$iso3 == "AFG",
  c("country", "inform_risk_rank", "top_10_proportion")
]

selected_long <- collate_indices(
  c("inform_risk", "hdi", "mpi"),
  format = "long"
)
subset(
  selected_long,
  iso3 == "AFG",
  select = c(index_name, score, rank, top_10)
)
}
\seealso{
\link{individual_indices}, \link{humanitarian_index_sources}
}
