% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/live-data.R
\name{globalvuln_data}
\alias{globalvuln_data}
\title{Retrieve bundled or latest approved globalvuln data}
\usage{
globalvuln_data(
  source = c("latest", "package"),
  version = NULL,
  indices = NULL,
  format = c("long", "wide")
)
}
\arguments{
\item{source}{Data source. \code{"latest"} reads the approved online board and
\code{"package"} reads the installed package snapshot offline.}

\item{version}{Optional version of the \code{humanitarian_indices} pin. Versions
are only supported for \code{source = "latest"}.}

\item{indices}{Optional character vector of index identifiers. The default
selects all supported indices.}

\item{format}{Output layout: \code{"long"} (the default) or \code{"wide"}.}
}
\value{
A data frame with provenance in the \code{globalvuln_source},
\code{globalvuln_version}, and \code{globalvuln_manifest} attributes.
}
\description{
\code{globalvuln_data()} makes data freshness explicit. Package data are the
immutable snapshot installed with \code{globalvuln}; latest data are read from
the approved, versioned static board published with the package website.
}
\examples{
package_data <- globalvuln_data(
  source = "package",
  indices = c("inform_risk", "hdi")
)
}
\seealso{
\code{\link[=collate_indices]{collate_indices()}}, \code{\link[=source_status]{source_status()}}
}
